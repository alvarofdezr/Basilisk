system:
  version: "6.6.0"
  log_level: "INFO"

monitoring:
  directories:
    - "./test_folder"
  log_file: "server_logs.txt"

database:
  name: "basilisk.db"

network:
  whitelist:
    - "chrome.exe"
    - "firefox.exe"
    - "msedge.exe"
    - "svchost.exe"
    - "python.exe"

# NOTA: Los secretos (API Keys, Passwords) deben configurarse en el archivo .env